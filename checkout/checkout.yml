spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/checkout
        username: postgres
        password: root
    jpa:
        show-sql: true
    properties:
        hibernate:
            dialect: org.hibernate.dialect.PostgreSQLDialect
            ddl-auto: update
server:
    port: 8082
